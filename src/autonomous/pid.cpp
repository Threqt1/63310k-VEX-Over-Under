#include "main.h"

// int MAX_SPEED = 12000;
// void moveForwardFor(QLength length)
// {
//     int kP = 0;
//     int kI = 0;
//     int kD = 0;

//     double totalDistance = length.convert(inch);
//     double currentDistance = 0_in;
//     double constrainingFactor = std::abs(totalDistance);
//     double currentError = (totalDistance - currentDistance) / constrainingFactor;
//     double lastError = currentError;
//     double currentSpeed = std::signbit(currentError) * MAX_SPEED;

//     while (std::abs(currentError) > .1)
//     {
//     }
// }